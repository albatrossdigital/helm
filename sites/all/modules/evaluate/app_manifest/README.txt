App Manifest

To use:

* At admin/structure/features, click on the link for a feature.
* Click the "Manifest" tab.
* Edit the resulting text and use it for the manifest field in an App server
app release node.

